# ==============================================================================
#  本仓库只提交「代码 + 示例模板」。
#  下面这些都是**每台机器各不相同**的运行时数据 / 私有配置，一律不进版本库。
#  模板见同目录的 config.example.json 与 .env.example。
# ==============================================================================

# ---- 私有配置与凭据 --------------------------------------------------------
config.json
.env
.env.*
!*.example

# ---- 运行时数据：状态库 / 日志 / 临时产物 ---------------------------------
state.db
state.db-wal
state.db-shm
state.db.bak-*
*.log
logs/
tmp/
_隔离/
_cloudtest/
_mineru_test/

# ---- 转换产物 --------------------------------------------------------------
# 默认 output.mode=alongside 时 md 落在语料目录，本来就不在仓库里；
# 万一把 output.root 配到了仓库内部（或有人手滑），这里挡住，避免提交上千个 md。
*.md
!/README.md
*.assets/

# ---- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
benv/
_benv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# ---- 编辑器 / 系统 ---------------------------------------------------------
.vscode/
.idea/
*.swp
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
